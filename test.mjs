import { createClient } from "@supabase/supabase-js"; const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); Promise.all([ supabase.from("candidates").select("id,first_name").or("first_name.ilike.%evo%"), supabase.from("candidates").select("id,first_name").or("first_name.ilike.*evo*") ]).then(console.log).catch(console.error);
